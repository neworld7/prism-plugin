# Loom Plugin Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Claude Code plugin that automates Google Stitch AI design tool via its official MCP server, providing bidirectional Code↔Design pipelines with verification loops.

**Architecture:** MCP-Only plugin following banani-automation patterns. All Stitch design operations use the official Remote MCP at `https://stitch.googleapis.com/mcp`. Stop hooks manage verification loops via YAML frontmatter state files.

**Tech Stack:** Claude Code Plugin (markdown/JSON/bash), Stitch Remote MCP (HTTP), gcloud OAuth ADC

**Spec:** `docs/superpowers/specs/2026-03-17-loom-design.md`
**Reference:** banani-automation plugin at `~/.claude/plugins/cache/banani-plugins/banani-automation/0.2.0/`

---

## File Map

| File | Responsibility |
|------|---------------|
| `.claude-plugin/plugin.json` | Plugin metadata (name, version, keywords) |
| `.claude-plugin/marketplace.json` | Marketplace distribution config |
| `.mcp.json` | Stitch Remote MCP server connection |
| `commands/loom.md` | `/loom` slash command with all subcommands |
| `skills/loom/SKILL.md` | Skill activation, core workflows, critical patterns |
| `skills/loom/references/tools.md` | 14 MCP tools reference with params and usage |
| `skills/loom/references/prompting.md` | Stitch prompting guide |
| `skills/loom/references/workflows-design.md` | Code→Design pipeline Phase 1-7 |
| `skills/loom/references/workflows-implement.md` | Design→Code pipeline Phase 1-7 |
| `skills/loom/references/sheet-template.md` | Design/implement sheet markdown template |
| `hooks/hooks.json` | Stop hook declarations |
| `hooks/scripts/design-verify-stop.sh` | Design verification loop controller |
| `hooks/scripts/code-verify-stop.sh` | Code verification loop controller |

---

## Chunk 1: Plugin Scaffold & MCP Config

### Task 1: Plugin Manifest Files

**Files:**
- Create: `.claude-plugin/plugin.json`
- Create: `.claude-plugin/marketplace.json`

- [ ] **Step 1: Create plugin.json**

```json
{
  "name": "loom",
  "version": "0.1.0",
  "description": "Google Stitch AI design tool automation plugin for Claude Code",
  "author": {
    "name": "Taekwan Kim"
  },
  "keywords": ["stitch", "design", "mcp", "automation", "ui-design", "google"]
}
```

- [ ] **Step 2: Create marketplace.json**

```json
{
  "name": "looms",
  "owner": {
    "name": "Taekwan Kim"
  },
  "metadata": {
    "description": "Google Stitch AI design tool automation plugins",
    "version": "0.1.0"
  },
  "plugins": [
    {
      "name": "loom",
      "source": "./",
      "description": "Google Stitch AI design tool automation — design pipeline, code pipeline, screen CRUD, design systems",
      "version": "0.1.0",
      "author": {
        "name": "Taekwan Kim"
      },
      "keywords": ["stitch", "design", "mcp", "automation"]
    }
  ]
}
```

- [ ] **Step 3: Verify directory structure**

Run: `ls -la .claude-plugin/`
Expected: `plugin.json` and `marketplace.json` both present

- [ ] **Step 4: Commit**

```bash
git add .claude-plugin/
git commit -m "feat: add plugin manifest files for loom"
```

### Task 2: MCP Server Configuration

**Files:**
- Create: `.mcp.json`

- [ ] **Step 1: Create .mcp.json**

```json
{
  "mcpServers": {
    "stitch": {
      "type": "http",
      "url": "https://stitch.googleapis.com/mcp"
    }
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add .mcp.json
git commit -m "feat: add Stitch Remote MCP server configuration"
```

---

## Chunk 2: Hook System

### Task 3: Hook Scripts

**Files:**
- Create: `hooks/hooks.json`
- Create: `hooks/scripts/design-verify-stop.sh`
- Create: `hooks/scripts/code-verify-stop.sh`

- [ ] **Step 1: Create hooks.json**

```json
{
  "Stop": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/design-verify-stop.sh",
          "timeout": 10
        },
        {
          "type": "command",
          "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/code-verify-stop.sh",
          "timeout": 10
        }
      ]
    }
  ]
}
```

- [ ] **Step 2: Create design-verify-stop.sh**

Adapt from banani's `design-verify-stop.sh` (reference: `~/.claude/plugins/cache/banani-plugins/banani-automation/0.2.0/hooks/scripts/design-verify-stop.sh`).

Key changes from banani:
- State file path: `.claude/loom-design-pipeline.local.md` (was `.claude/design-pipeline.local.md`)
- Re-injection prompt references Stitch MCP tools instead of chrome-viewer
- Prompt says "Stitch MCP의 get_screen + web_fetch" instead of "cv_screenshot"

```bash
#!/usr/bin/env bash
# Stop hook: Stitch design pipeline verification loop
#
# When phase is "verify", checks if <promise>DESIGN_VERIFIED</promise>
# appeared in the transcript. If not, re-injects verification prompt.
#
# State file: .claude/loom-design-pipeline.local.md (YAML frontmatter)

set -euo pipefail

# --- Config ---
STATE_FILE=".claude/loom-design-pipeline.local.md"
COMPLETION_PROMISE="DESIGN_VERIFIED"
DEFAULT_MAX_ITERATIONS=5

# --- Read hook input from stdin ---
INPUT=$(cat)

# --- Guard: state file must exist ---
if [ ! -f "$STATE_FILE" ]; then
  exit 0
fi

# --- Parse state file frontmatter ---
get_field() {
  local field="$1"
  sed -n '/^---$/,/^---$/p' "$STATE_FILE" | grep "^${field}:" | head -1 | sed "s/^${field}:[[:space:]]*//"
}

PHASE=$(get_field "phase")
SESSION_ID=$(get_field "session_id")
ITERATION=$(get_field "iteration")
MAX_ITER=$(get_field "max_iterations")

# Defaults
ITERATION="${ITERATION:-0}"
MAX_ITER="${MAX_ITER:-$DEFAULT_MAX_ITERATIONS}"

# --- Guard: only act during verify phase ---
if [ "$PHASE" != "verify" ]; then
  exit 0
fi

# --- Guard: session_id match (if set) ---
if [ -n "$SESSION_ID" ]; then
  CURRENT_SESSION=$(echo "$INPUT" | grep -o '"session_id"[[:space:]]*:[[:space:]]*"[^"]*"' | head -1 | sed 's/.*"session_id"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/' 2>/dev/null || true)
  if [ -n "$CURRENT_SESSION" ] && [ "$CURRENT_SESSION" != "$SESSION_ID" ]; then
    exit 0
  fi
fi

# --- Check for completion promise in transcript ---
if echo "$INPUT" | grep -q "<promise>${COMPLETION_PROMISE}</promise>" 2>/dev/null; then
  rm -f "$STATE_FILE"
  echo '{"decision": "allow"}'
  exit 0
fi

# --- Check iteration limit ---
NEXT_ITER=$((ITERATION + 1))
if [ "$NEXT_ITER" -gt "$MAX_ITER" ]; then
  sed -i '' "s/^phase:.*/phase: done_max_iter/" "$STATE_FILE" 2>/dev/null || true
  echo '{"decision": "allow"}'
  exit 0
fi

# --- Update iteration count in state file ---
if grep -q "^iteration:" "$STATE_FILE"; then
  sed -i '' "s/^iteration:.*/iteration: ${NEXT_ITER}/" "$STATE_FILE"
else
  sed -i '' "/^---$/a\\
iteration: ${NEXT_ITER}" "$STATE_FILE"
fi

# --- Re-inject verification prompt ---
cat <<'HOOK_OUTPUT'
{
  "decision": "block",
  "reason": "디자인 검증 루프를 계속합니다.\n\n1. Read .claude/loom-design-pipeline.local.md → 남은 gaps 확인\n2. gaps > 0: Stitch MCP edit_screens로 누락분 수정 또는 generate_screen_from_text로 재생성 → Phase 5 재검증\n3. gaps == 0: <promise>DESIGN_VERIFIED</promise> 출력\n\nreferences/workflows-design.md Phase 5 절차를 따르세요."
}
HOOK_OUTPUT
```

- [ ] **Step 3: Create code-verify-stop.sh**

Adapt from banani's `code-verify-stop.sh`.

Key changes:
- State file path: `.claude/loom-implement-pipeline.local.md` (was `.claude/design-to-code.local.md`)
- Re-injection prompt references Stitch MCP for design screenshots

```bash
#!/usr/bin/env bash
# Stop hook: Stitch design-to-code verification loop
#
# When phase is "code_verify", checks if <promise>CODE_VERIFIED</promise>
# appeared in the transcript. If not, re-injects verification prompt.
#
# State file: .claude/loom-implement-pipeline.local.md (YAML frontmatter)

set -euo pipefail

# --- Config ---
STATE_FILE=".claude/loom-implement-pipeline.local.md"
COMPLETION_PROMISE="CODE_VERIFIED"
DEFAULT_MAX_ITERATIONS=5

# --- Read hook input from stdin ---
INPUT=$(cat)

# --- Guard: state file must exist ---
if [ ! -f "$STATE_FILE" ]; then
  exit 0
fi

# --- Parse state file frontmatter ---
get_field() {
  local field="$1"
  sed -n '/^---$/,/^---$/p' "$STATE_FILE" | grep "^${field}:" | head -1 | sed "s/^${field}:[[:space:]]*//"
}

PHASE=$(get_field "phase")
SESSION_ID=$(get_field "session_id")
ITERATION=$(get_field "iteration")
MAX_ITER=$(get_field "max_iterations")
TARGET_STACK=$(get_field "target_stack")

# Defaults
ITERATION="${ITERATION:-0}"
MAX_ITER="${MAX_ITER:-$DEFAULT_MAX_ITERATIONS}"
TARGET_STACK="${TARGET_STACK:-flutter}"

# --- Guard: only act during code_verify phase ---
if [ "$PHASE" != "code_verify" ]; then
  exit 0
fi

# --- Guard: session_id match (if set) ---
if [ -n "$SESSION_ID" ]; then
  CURRENT_SESSION=$(echo "$INPUT" | grep -o '"session_id"[[:space:]]*:[[:space:]]*"[^"]*"' | head -1 | sed 's/.*"session_id"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/' 2>/dev/null || true)
  if [ -n "$CURRENT_SESSION" ] && [ "$CURRENT_SESSION" != "$SESSION_ID" ]; then
    exit 0
  fi
fi

# --- Check for completion promise in transcript ---
if echo "$INPUT" | grep -q "<promise>${COMPLETION_PROMISE}</promise>" 2>/dev/null; then
  rm -f "$STATE_FILE"
  echo '{"decision": "allow"}'
  exit 0
fi

# --- Check iteration limit ---
NEXT_ITER=$((ITERATION + 1))
if [ "$NEXT_ITER" -gt "$MAX_ITER" ]; then
  sed -i '' "s/^phase:.*/phase: done_max_iter/" "$STATE_FILE" 2>/dev/null || true
  echo '{"decision": "allow"}'
  exit 0
fi

# --- Update iteration count in state file ---
if grep -q "^iteration:" "$STATE_FILE"; then
  sed -i '' "s/^iteration:.*/iteration: ${NEXT_ITER}/" "$STATE_FILE"
else
  sed -i '' "/^---$/a\\
iteration: ${NEXT_ITER}" "$STATE_FILE"
fi

# --- Re-inject verification prompt ---
cat <<HOOK_OUTPUT
{
  "decision": "block",
  "reason": "코드 검증 루프를 계속합니다.\\n\\n1. Read .claude/loom-implement-pipeline.local.md → 남은 화면과 차이 확인\\n2. Stitch MCP get_screen + web_fetch로 디자인 스크린샷 수집\\n3. 구현 코드 스크린샷 촬영 (${TARGET_STACK})\\n4. diffs > 0: 코드 수정 → Phase 5 재검증\\n5. diffs == 0 (또는 LOW만 남음): <promise>CODE_VERIFIED</promise> 출력\\n\\nreferences/workflows-implement.md Phase 5 절차를 따르세요."
}
HOOK_OUTPUT
```

- [ ] **Step 4: Make scripts executable**

Run: `chmod +x hooks/scripts/design-verify-stop.sh hooks/scripts/code-verify-stop.sh`

- [ ] **Step 5: Validate hook scripts**

Run: `bash -n hooks/scripts/design-verify-stop.sh && bash -n hooks/scripts/code-verify-stop.sh && echo "OK"`
Expected: `OK` (no syntax errors)

- [ ] **Step 6: Validate hooks.json**

Run: `python3 -c "import json; json.load(open('hooks/hooks.json')); print('OK')"`
Expected: `OK`

- [ ] **Step 7: Commit**

```bash
git add hooks/
git commit -m "feat: add Stop hook scripts for design and code verification loops"
```

---

## Chunk 3: Skill System

### Task 4: SKILL.md

**Files:**
- Create: `skills/loom/SKILL.md`

Reference: banani's `SKILL.md` at `~/.claude/plugins/cache/banani-plugins/banani-automation/0.2.0/skills/banani-automation/SKILL.md`

- [ ] **Step 1: Create SKILL.md**

```markdown
---
name: loom
description: "Google Stitch AI design tool automation. Design pipeline, code pipeline, screen CRUD, design systems, variants. Use when user mentions stitch, 스티치, stitch.withgoogle.com, Stitch 디자인, Stitch MCP."
---

# Stitch Automation Skill

Automate Google Stitch AI design tool via the official Stitch Remote MCP server.

## When to Use

Activate when user:
- Mentions "stitch", "스티치", "stitch.withgoogle.com"
- Asks to create/edit design screens using Stitch
- Wants to generate design variants or manage design systems
- References AI UI design with Stitch
- Asks for UI/UX 디자인, 화면 디자인, 디자인 생성 (in Stitch context)

## Prerequisites

### Authentication Check

Before any Stitch MCP operation, verify auth:
```bash
gcloud auth application-default print-access-token 2>/dev/null | head -c 20
```

If token fetch fails, prompt user:
> Stitch MCP 인증이 필요합니다. 아래 명령을 순서대로 실행해주세요:
> 1. `gcloud auth login`
> 2. `gcloud auth application-default login`

### MCP Tool Availability

Verify Stitch MCP tools are loaded:
```
ToolSearch query: "+stitch list_projects"
```

If not found, the `.mcp.json` in this plugin should auto-register the Stitch MCP server.

## Design Pipeline (코드→디자인 워크플로우)

Stitch의 주 용도는 **코드→프로덕션 디자인 전환 파이프라인**이다.
`/loom design` 또는 디자인 관련 요청 시 아래 7단계를 따른다.

| Phase | 내용 | 핵심 |
|-------|------|------|
| 1 | 코드 분석 | Glob/Grep으로 화면·인터랙션·상태 전수 조사 |
| 2 | 디자인 시트 작성 | `docs/plans/` 에 코드 vs 디자인 비교 문서 |
| 3 | 프롬프트 최적화 | Stitch에 최적화된 프롬프트 작성 |
| 4 | Stitch 디자인 생성 | MCP로 프로젝트/화면/디자인시스템 생성 |
| 5 | 검증 | 코드 ↔ Stitch 크로스체크, gaps 카운트 |
| 6 | 누락분 수정 | gaps > 0이면 edit_screens 또는 재생성 |
| 7 | 루프 | 5→6 반복, gaps == 0이면 종료 |

### Pipeline 실행 시 반드시 참조

```
Read: references/workflows-design.md   ← Phase별 실행 절차 상세
Read: references/sheet-template.md      ← 디자인 시트 마크다운 템플릿
Read: references/prompting.md           ← Stitch 프롬프팅 가이드
```

Phase 5-7 검증 루프는 **Stop hook**이 자동 관리한다.
상태 파일 `.claude/loom-design-pipeline.local.md`에 `phase: verify`가 설정되면
Stop hook이 `<promise>DESIGN_VERIFIED</promise>` 감지까지 루프를 반복한다.

### 완료 조건
- [ ] 코드의 모든 화면이 Stitch 디자인에 1:1 매핑
- [ ] 모든 버튼/인터랙션이 디자인에 반영
- [ ] 상태별 화면(로딩, 에러, 엠티)이 포함
- [ ] 검증 루프에서 누락 0건 확인

## Implement Pipeline (디자인→코드 워크플로우)

Stitch 디자인을 실제 코드에 반영하는 **역방향 파이프라인**.
`/loom implement` 또는 "디자인을 코드로", "코드에 반영" 요청 시 아래 7단계를 따른다.

| Phase | 내용 | 핵심 |
|-------|------|------|
| 1 | 디자인 수집 | Stitch MCP로 프로젝트/스크린 데이터 수집 |
| 2 | 코드 매핑 | Stitch 스크린 ↔ 기존 코드 파일 대응표 작성 |
| 3 | 구현 계획 | 코드 시트 작성 (화면별 변환 전략) → 사용자 확인 |
| 4 | 코드 구현 | 시트 기반으로 Flutter/React 코드 작성/수정 |
| 5 | 시각 검증 | 구현 코드 스크린샷 ↔ Stitch 디자인 스크린샷 비교 |
| 6 | 차이 수정 | 불일치 항목 코드 수정 |
| 7 | 루프 | 5→6 반복, 차이 0이면 종료 |

### Pipeline 실행 시 반드시 참조

```
Read: references/workflows-implement.md   ← Phase별 실행 절차 상세
Read: references/tools.md                 ← MCP 도구 파라미터 참조
```

Phase 5-7 검증 루프는 **Stop hook**이 자동 관리한다.
상태 파일 `.claude/loom-implement-pipeline.local.md`에 `phase: code_verify`가 설정되면
Stop hook이 `<promise>CODE_VERIFIED</promise>` 감지까지 루프를 반복한다.

### 시각 검증 핵심

1. **Stitch 디자인 스크린샷**: `get_screen` → `web_fetch(downloadUrl)`
2. **구현 코드 스크린샷**:
   - Flutter: `xcrun simctl io booted screenshot /tmp/{screen}.png` + `sips -Z 1200`
   - React/Next.js: chrome-viewer `cv_screenshot` 또는 Playwright `browser_take_screenshot`
3. **비교**: 두 이미지를 나란히 Read → 레이아웃/색상/컴포넌트/콘텐츠 차이 분석
4. **차이 리포트**: HIGH(누락)/MED(구조)/LOW(폴리싱) 분류
5. **코드 수정 후 재검증**: diffs == 0이면 `<promise>CODE_VERIFIED</promise>`

### 완료 조건
- [ ] 모든 대상 화면이 코드로 구현됨
- [ ] 구현 코드 스크린샷이 Stitch 디자인과 시각적 일치
- [ ] HIGH/MED 차이 0건
- [ ] 코드 시트의 모든 항목이 [DONE]

## Critical Patterns

### Pattern 1: Screen Data Retrieval

Stitch MCP의 `get_screen`은 `downloadUrls`를 반환한다.
HTML 코드와 스크린샷 이미지를 실제로 가져오려면 `web_fetch`가 필요:

```
1. get_screen(projectId, screenId) → response with downloadUrls
2. web_fetch(response.downloadUrls.html) → HTML/CSS code
3. web_fetch(response.downloadUrls.screenshot) → screenshot image
```

### Pattern 2: Screenshot Discipline

- **MCP 도구 우선**: 가능하면 `get_screen` 데이터로 상태 확인
- **스크린샷**: 시각 검증 마일스톤에서만 사용
- **리사이즈 필수**: `sips -Z 1200 <file>` (컨텍스트 오버플로우 방지)

### Pattern 3: Design System Consistency

여러 화면 생성 시 디자인 일관성 유지:
```
1. create_design_system → 테마 정의
2. generate_screen_from_text → 화면 생성
3. apply_design_system → 생성된 화면에 디자인 시스템 적용
```

### Pattern 4: Error Recovery

- MCP 도구 실패 → 3회까지 재시도 (1s, 2s, 4s backoff)
- Auth 만료 → `gcloud auth application-default login` 재실행 안내
- Rate limit → Stitch 생성 한도 안내 (Standard 350/month, Experimental 50/month)

## Workflow Reference

| Task | Reference File |
|------|----------------|
| Code → Design pipeline (코드를 디자인으로) | `references/workflows-design.md` |
| Design → Code pipeline (디자인을 코드로) | `references/workflows-implement.md` |
| Stitch prompting best practices | `references/prompting.md` |
| MCP tool parameters & usage | `references/tools.md` |
| Design/implement sheet template | `references/sheet-template.md` |
```

- [ ] **Step 2: Commit**

```bash
git add skills/loom/SKILL.md
git commit -m "feat: add SKILL.md with activation triggers and workflow definitions"
```

### Task 5: MCP Tools Reference

**Files:**
- Create: `skills/loom/references/tools.md`

- [ ] **Step 1: Create tools.md**

Content: Complete reference for all 14 Stitch MCP tools with parameters, return values, and usage examples.

Source this from:
- The MCP Reference page at `https://stitch.withgoogle.com/docs/mcp/reference`
- The `npx @_davideast/stitch-mcp tool <name> -s` output for each tool

Include for each tool:
- Name, description, category
- Input parameters (name, type, required/optional, description)
- Return value structure
- Usage example
- Important notes (e.g., `projectId` must be numeric string without `projects/` prefix)

This is the longest reference file. Key tools to document thoroughly:
- `generate_screen_from_text` (prompt, projectId, deviceType, modelId)
- `edit_screens` (projectId, screenIds, prompt)
- `create_design_system` / `apply_design_system`
- `get_screen` (downloadUrls pattern)

- [ ] **Step 2: Commit**

```bash
git add skills/loom/references/tools.md
git commit -m "feat: add MCP tools reference with all 14 Stitch tools"
```

### Task 6: Prompting Guide Reference

**Files:**
- Create: `skills/loom/references/prompting.md`

- [ ] **Step 1: Create prompting.md**

Content sourced from official Stitch docs (`stitch.withgoogle.com/docs/learn/prompting/`):

Cover these sections:
1. **Starting Your Project** — define purpose, list UI components, mention layout, specify style/theme, include dynamic content, add branding
2. **High-Level vs Detailed Prompts** — when to use each
3. **Setting the Vibe with Adjectives** — atmosphere keywords that Stitch understands
4. **Refining Screen by Screen** — iterating per-screen with specific changes
5. **Controlling App Theme** — colors, fonts, borders
6. **Describing Desired Imagery** — how to specify images
7. **Pro Tips** — prompt length, specificity, device-aware tips
8. **Flutter-specific prompting** — prompts that generate mobile-first layouts suitable for Flutter conversion

- [ ] **Step 2: Commit**

```bash
git add skills/loom/references/prompting.md
git commit -m "feat: add Stitch prompting guide reference"
```

- [ ] **Step 3: Commit chunk 3**

```bash
git add skills/
git commit -m "feat: add skill system with SKILL.md and reference files"
```

---

## Chunk 4: Workflow References

### Task 7: Code→Design Pipeline Workflow

**Files:**
- Create: `skills/loom/references/workflows-design.md`

- [ ] **Step 1: Create workflows-design.md**

Content: Detailed Phase 1-7 execution steps for the Code→Design pipeline.

Structure each phase with:
- **Goal** — what this phase achieves
- **Steps** — exact sequence of actions (MCP calls, file operations)
- **Output** — what gets produced
- **Transition** — when to move to next phase

Key sections:

**Phase 1: Code Analysis**
- Glob patterns for Flutter (`lib/**/*.dart`), React (`src/**/*.{tsx,jsx}`), Next.js (`app/**/*.{tsx,jsx}`)
- Grep patterns for routes, navigation, state management
- Output: screen list, interaction list, state list

**Phase 2: Design Sheet**
- Reference `sheet-template.md` for format
- Save to `docs/plans/{date}-{feature}-design-sheet.md`
- **User confirmation gate** before Phase 3

**Phase 3: Prompt Optimization**
- Reference `prompting.md` for best practices
- Per-screen prompt drafting
- Device type selection (mobile → Flutter, desktop → React/Next.js)

**Phase 4: Stitch Design Generation**
```
1. create_project(title: "{feature} Design")
2. create_design_system(projectId, theme: {...})
3. For each screen:
   generate_screen_from_text(projectId, prompt, deviceType)
4. apply_design_system(projectId, designSystemId)
5. Optional: generate_variants(projectId, screenIds, prompt)
```

**Phase 5: Verification**
```
1. For each screen in design sheet:
   get_screen(projectId, screenId) → downloadUrls
   web_fetch(downloadUrl) → screenshot
   Read screenshot → visual check
2. Cross-check against design sheet checklist
3. Count gaps: MISSING_SCREEN, MISSING_INTERACTION, MISSING_STATE
4. Record gaps in state file
```

**Phase 6: Fix**
```
If gaps > 0:
  edit_screens(projectId, screenIds, fixPrompt)
  OR generate_screen_from_text(projectId, newPrompt)
Return to Phase 5
```

**Phase 7: Completion**
```
If gaps == 0:
  Output: <promise>DESIGN_VERIFIED</promise>
  Update design sheet with final results
  State file auto-cleaned by Stop hook
```

- [ ] **Step 2: Commit**

```bash
git add skills/loom/references/workflows-design.md
git commit -m "feat: add Code→Design pipeline workflow reference"
```

### Task 8: Design→Code Pipeline Workflow

**Files:**
- Create: `skills/loom/references/workflows-implement.md`

- [ ] **Step 1: Create workflows-implement.md**

Content: Detailed Phase 1-7 execution steps for the Design→Code pipeline.

Key sections:

**Phase 1: Stitch Design Collection**
```
1. list_projects(view: "owned") → select project
2. list_screens(projectId) → enumerate screens
3. For each screen:
   get_screen(projectId, screenId) → metadata + downloadUrls
   web_fetch(downloadUrl.html) → save HTML to /tmp/loom-{screenName}.html
   web_fetch(downloadUrl.screenshot) → save image to /tmp/loom-{screenName}.png
```

**Phase 2: Code Mapping**
- Explore existing code (Glob/Grep)
- Build mapping table: Stitch screen name → code file path
- Classify: `[NEW]` (no code exists), `[EDIT]` (code exists, needs update), `[OK]` (already matches)

**Phase 3: Implementation Plan Sheet**
- Reference `sheet-template.md` (implement variant)
- Per-screen conversion strategy:
  - Flutter: HTML structure → Widget tree mapping rules
  - React/Next.js: HTML nearly as-is + component splitting
- **User confirmation gate** before Phase 4

**Phase 4: Code Implementation**
- For each `[NEW]` screen: create file, implement from Stitch HTML
- For each `[EDIT]` screen: modify existing code to match Stitch design
- Follow target stack conventions (Flutter/React/Next.js)

**Phase 5: Visual Verification**
```
1. For each implemented screen:
   # Get Stitch design screenshot
   get_screen(projectId, screenId) → downloadUrls
   web_fetch(downloadUrl.screenshot) → /tmp/loom-{screen}.png
   sips -Z 1200 /tmp/loom-{screen}.png

   # Get implementation screenshot
   Flutter: xcrun simctl io booted screenshot /tmp/impl-{screen}.png
   React: cv_screenshot or playwright browser_take_screenshot

   sips -Z 1200 /tmp/impl-{screen}.png

   # Compare
   Read both images → analyze differences
   Classify: HIGH/MED/LOW

2. Aggregate diffs across all screens
```

**Phase 6-7: Fix & Loop** — same pattern as design pipeline

- [ ] **Step 2: Commit**

```bash
git add skills/loom/references/workflows-implement.md
git commit -m "feat: add Design→Code pipeline workflow reference"
```

### Task 9: Design Sheet Template

**Files:**
- Create: `skills/loom/references/sheet-template.md`

- [ ] **Step 1: Create sheet-template.md**

Adapt from banani's `sheet-template.md`. Key changes:
- Replace "banani" references with "Stitch"
- Replace "banani 스크린" columns with "Stitch 스크린"
- Add `project_id` field
- Add `target_stack` field for implement sheets
- Add `design_system_id` field

Include two template variants:
1. **Design sheet** (for `/loom design` pipeline)
2. **Implement sheet** (for `/loom implement` pipeline)

- [ ] **Step 2: Commit**

```bash
git add skills/loom/references/sheet-template.md
git commit -m "feat: add design/implement sheet templates"
```

---

## Chunk 5: Command System

### Task 10: /loom Slash Command

**Files:**
- Create: `commands/loom.md`

Reference: banani's `commands/banani.md`

- [ ] **Step 1: Create stitch.md**

```markdown
---
name: stitch
description: "Google Stitch AI design tool automation — design pipeline, code pipeline, screen CRUD, design systems, variants"
---

# /loom Command

Google Stitch AI design tool automation command.

## Usage

Parse user arguments to determine the subcommand:

| Subcommand | Usage | Action |
|------------|-------|--------|
| `design` | `/loom design [feature]` | **코드→디자인 파이프라인** (Phase 1-7, 디자인 검증 루프 포함) |
| `implement` | `/loom implement [feature]` | **디자인→코드 파이프라인** (Phase 1-7, 시각 검증 루프 포함) |
| `verify-loop` | `/loom verify-loop` | 디자인 검증 루프만 단독 시작 |
| `code-verify-loop` | `/loom code-verify-loop` | 코드 검증 루프만 단독 시작 |
| `cancel-loop` | `/loom cancel-loop` | 진행 중인 검증 루프 즉시 취소 |
| `status` | `/loom status` | 현재 파이프라인 상태 표시 |
| `list` | `/loom list` | 프로젝트/화면 목록 |
| `create` | `/loom create '프로젝트명' 또는 '프롬프트'` | 프로젝트 또는 화면 생성 |
| `edit` | `/loom edit '화면명' '수정 프롬프트'` | 화면 편집 |
| `variants` | `/loom variants '화면명'` | 디자인 변형 생성 |
| `theme` | `/loom theme` | 디자인 시스템 생성/적용 |
| `export` | `/loom export html\|image` | 화면 HTML/이미지 다운로드 |

## `/loom design [feature]` — 전체 파이프라인

가장 핵심적인 서브커맨드. Phase 1-7을 자동 실행한다.

### 실행 절차

1. **상태 파일 초기화**: `.claude/loom-design-pipeline.local.md` 생성
   ```yaml
   ---
   phase: analysis
   feature: {feature 또는 "all"}
   session_id: {현재 세션 ID}
   iteration: 0
   max_iterations: 5
   ---
   ```

2. **파이프라인 레퍼런스 로드**:
   ```
   Read: references/workflows-design.md
   Read: references/sheet-template.md
   Read: references/prompting.md
   ```

3. **Phase 1-2**: 코드 분석 → 디자인 시트 작성 → **사용자 확인 요청**

4. **Phase 3**: 프롬프트 최적화

5. **Phase 4**: Stitch MCP로 디자인 생성
   - `create_project` → `create_design_system` → `generate_screen_from_text` × N → `apply_design_system`

6. **Phase 4 완료 후**: 상태 파일의 `phase`를 `verify`로 변경 → 검증 루프 자동 시작
   - Stop hook이 `<promise>DESIGN_VERIFIED</promise>` 감지까지 루프 반복

`feature` 인자 예시: `/loom design library`, `/loom design dashboard`
인자 없으면 전체 앱 대상 (feature 선택 프롬프트 표시).

## `/loom implement [feature]` — 디자인→코드 파이프라인

Stitch 디자인을 실제 코드에 반영하는 **역방향 파이프라인**. Phase 1-7을 자동 실행한다.

### 실행 절차

1. **상태 파일 초기화**: `.claude/loom-implement-pipeline.local.md` 생성
   ```yaml
   ---
   phase: collect
   feature: {feature 또는 "all"}
   session_id: {현재 세션 ID}
   iteration: 0
   max_iterations: 5
   target_stack: flutter
   ---
   ```

2. **파이프라인 레퍼런스 로드**:
   ```
   Read: references/workflows-implement.md
   Read: references/tools.md
   ```

3. **Phase 1-2**: Stitch 디자인 수집 + 코드 매핑

4. **Phase 3**: 코드 시트 작성 → `docs/plans/` 에 생성 → **사용자 확인 요청**

5. **Phase 4**: 시트 기반으로 코드 작성/수정

6. **Phase 4 완료 후**: 상태 파일의 `phase`를 `code_verify`로 변경 → 시각 검증 루프 자동 시작

## `/loom verify-loop` — 디자인 검증 루프 단독

1. `.claude/loom-design-pipeline.local.md` 생성 (phase: verify)
2. `Read: references/workflows-design.md` Phase 5 절차 실행
3. Stop hook이 자동으로 루프 관리

## `/loom code-verify-loop` — 코드 검증 루프 단독

1. `.claude/loom-implement-pipeline.local.md` 생성 (phase: code_verify)
2. `Read: references/workflows-implement.md` Phase 5 절차 실행
3. Stop hook이 자동으로 루프 관리

## `/loom cancel-loop` — 루프 취소

```bash
rm -f .claude/loom-design-pipeline.local.md .claude/loom-implement-pipeline.local.md
```
"검증 루프가 취소되었습니다." 메시지 출력

## `/loom status` — 상태 확인

두 상태 파일을 읽어 현재 상태 표시:
- 현재 phase, feature, project_id
- iteration / max_iterations
- target_stack (implement only)

상태 파일이 없으면 "활성 파이프라인 없음" 출력.

## `/loom list` — 목록

```
list_projects(view: "owned") → 프로젝트 목록 표시
```
프로젝트 선택 후 `list_screens(projectId)` → 화면 목록 표시

## `/loom create` — 생성

프로젝트 또는 화면 생성:
- 프로젝트: `create_project(title: "...")`
- 화면: `generate_screen_from_text(projectId, prompt, deviceType)`

## `/loom edit` — 편집

`edit_screens(projectId, screenIds: [...], prompt: "...")`

## `/loom variants` — 변형

`generate_variants(projectId, selectedScreenIds: [...], prompt, variantOptions)`

## `/loom theme` — 디자인 시스템

1. `list_design_systems(projectId)` → 기존 시스템 확인
2. `create_design_system(...)` 또는 `update_design_system(...)`
3. `apply_design_system(projectId, designSystemId)`

## `/loom export` — 내보내기

- HTML: `get_screen(projectId, screenId)` → `web_fetch(downloadUrl.html)` → 파일 저장
- Image: `get_screen(projectId, screenId)` → `web_fetch(downloadUrl.screenshot)` → 파일 저장

## Execution

1. Activate the `loom` skill — it contains all MCP tool patterns, workflows, and safety patterns.
2. Execute the requested subcommand following the skill's workflow references.

## No Arguments

If called without arguments (`/loom`), show the usage table above and ask what the user wants to do.

## Error Handling

- If MCP auth fails: prompt user to run `gcloud auth application-default login`
- If project/screen not found: show list and ask user to select
- If generation fails: retry up to 3 times, then report error
- If rate limit hit: inform user of Stitch limits and pause
```

- [ ] **Step 2: Commit**

```bash
git add commands/loom.md
git commit -m "feat: add /loom slash command with all subcommands"
```

---

## Chunk 6: Final Validation & Cleanup

### Task 11: Validate Complete Plugin Structure

- [ ] **Step 1: Verify all files exist**

Run: `find . -type f -not -path './.git/*' -not -path './docs/*' -not -path './.superpowers/*' -not -name 'firebase-debug.log' | sort`

Expected output:
```
./.claude-plugin/marketplace.json
./.claude-plugin/plugin.json
./.mcp.json
./commands/loom.md
./hooks/hooks.json
./hooks/scripts/code-verify-stop.sh
./hooks/scripts/design-verify-stop.sh
./skills/loom/SKILL.md
./skills/loom/references/prompting.md
./skills/loom/references/sheet-template.md
./skills/loom/references/tools.md
./skills/loom/references/workflows-design.md
./skills/loom/references/workflows-implement.md
```

- [ ] **Step 2: Validate all JSON files**

Run: `for f in .claude-plugin/plugin.json .claude-plugin/marketplace.json .mcp.json hooks/hooks.json; do python3 -c "import json; json.load(open('$f')); print('OK: $f')"; done`

Expected: All 4 files return OK

- [ ] **Step 3: Validate all bash scripts**

Run: `bash -n hooks/scripts/design-verify-stop.sh && bash -n hooks/scripts/code-verify-stop.sh && echo "All scripts OK"`

Expected: `All scripts OK`

- [ ] **Step 4: Add .gitignore**

```
.superpowers/
firebase-debug.log
```

- [ ] **Step 5: Final commit**

```bash
git add .gitignore
git commit -m "chore: add .gitignore and complete plugin structure"
```

### Task 12: gcloud Authentication Setup

This task requires user interaction (browser OAuth).

- [ ] **Step 1: Check current gcloud auth**

Run: `gcloud auth application-default print-access-token 2>&1 | head -c 20`

If fails, prompt user to run:
```bash
gcloud auth login
gcloud auth application-default login
```

- [ ] **Step 2: Register Stitch MCP with Claude Code**

Run: `claude mcp add stitch --transport http https://stitch.googleapis.com/mcp`

- [ ] **Step 3: Verify MCP connection**

Test with: `list_projects` tool call — should return a list (possibly empty) without auth errors.

- [ ] **Step 4: Create test project**

Test full round-trip:
```
create_project(title: "Plugin Test")
generate_screen_from_text(projectId: "...", prompt: "A simple login screen", deviceType: "MOBILE")
get_screen(projectId: "...", screenId: "...")
delete_project(name: "projects/...")
```

If all succeed, the plugin is ready for use.
