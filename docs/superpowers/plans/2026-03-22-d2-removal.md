# D2 제거 + Design System Name Anchor 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** D2 단계를 제거하고, Stitch 네이티브 디자인 시스템 이름을 앵커로 활용하여 파이프라인을 단순화한다.

**Architecture:** 4개 파일을 순차 수정. workflows.md(D2 제거 + D3 확장)가 핵심이고, SKILL.md/commands/stop hook은 이에 맞춰 동기화.

**Tech Stack:** Markdown (스킬/워크플로우 문서), Bash (Stop hook 스크립트)

**Spec:** `docs/superpowers/specs/2026-03-22-d2-removal-design.md`

**Note:** Task 1-4는 모두 `workflows.md`를 수정한다. 줄 번호는 **원본 파일 기준**이다. Task 1 이후 줄 번호가 시프트되므로, 구현 시 줄 번호가 아닌 **텍스트 패턴 매칭**으로 편집 위치를 찾아야 한다.

---

### Task 1: workflows.md — D2 섹션 제거

**Files:**
- Modify: `skills/prism/references/workflows.md:496-523` (D2 전체 섹션)

- [ ] **Step 1: D2 섹션을 제거하고 Design Identity 판단 로직으로 교체**

`### D2: 디자인 시스템 — DESIGN.md 일관성 보장` (496-523번 줄)을 삭제하고 아래로 교체:

```markdown
### D2: (제거됨 — Phase 번호 호환성을 위해 유지)

> D2는 v2.10.0에서 제거되었다. Stitch가 첫 화면 생성 시 디자인 시스템을 자동 생성하므로 외부 DESIGN.md 생성이 불필요하다.
> 디자인 시스템 일관성은 D3의 "Design System Name Anchor" 패턴으로 보장한다.
> 기존 D3-D6 번호를 유지한다.
```

- [ ] **Step 2: 변경 확인**

`workflows.md`에서 `design-md` 텍스트가 D2 이외에 남아있는지 확인:
```
Grep: "design-md" in skills/prism/references/workflows.md
```
A5 섹션에 남아있으면 Task 3에서 처리.

- [ ] **Step 3: Commit**

```bash
git add skills/prism/references/workflows.md
git commit -m "refactor: remove D2 section from workflows.md"
```

---

### Task 2: workflows.md — D3에 이름 추출 단계 추가

**Files:**
- Modify: `skills/prism/references/workflows.md:525-577` (D3 섹션)

- [ ] **Step 1: D3 Goal 뒤에 Design Identity 판단 + 추출 로직 삽입**

D3 `**실행:**` 블록(543번 줄 부근) 앞에 아래 내용 삽입:

```markdown
**Design Identity 판단 (D3 시작 시):**

`.prism/directions/{direction}/design-identity.md` 존재 여부로 분기:

**미존재 (첫 Feature 또는 새 Direction):**
```
1. create_project → 첫 화면 generate_screen_from_text
2. get_project → designTheme에서 이름 추출:
   - 소스 1: outputComponents 텍스트에서 '{NAME}' design system/aesthetic/palette 패턴
   - 소스 2: designTheme.designMd 첫 # 헤딩 파싱
   - Fallback: 소스 1 실패 → get_project로 소스 2 / 소스 2도 실패 → 2번째 화면 후 재시도 / 2회 실패 → 앵커 없이 진행
3. design-identity.md 저장:
   | 항목 | 값 |
   |------|------|
   | Name | {추출된 이름} |
   | Source Project | projects/{projectId} |
   | Color Mode | {designTheme.colorMode} |
   | Roundness | {designTheme.roundness} |
   | Primary Font | {designTheme.font 또는 headlineFont} |
   | Body Font | {designTheme.bodyFont} |
4. 나머지 화면 생성 (프롬프트 끝에 앵커 삽입):
   Continue using the "{Name}" design system established in this project.
```

**존재 (이후 Feature, 같은 Direction):**
```
1. Read design-identity.md → Name, Color Mode, Primary Font, Roundness 추출
2. 모든 화면 프롬프트에 앵커 삽입:
   Use the same "{Name}" design system — {Color Mode} mode, {Primary Font} typography, {Roundness} corners.
3. create_project → generate_screen_from_text (전체 화면 순차)
```
```

- [ ] **Step 2: Commit**

```bash
git add skills/prism/references/workflows.md
git commit -m "feat: add Design Identity extraction logic to D3"
```

---

### Task 3: workflows.md — A5 DESIGN.md 참조 제거 + A4 D2 참조 업데이트

**Files:**
- Modify: `skills/prism/references/workflows.md:309-395` (A4-A5 섹션)

- [ ] **Step 1: A4 원시 프롬프트 규칙 업데이트**

312번 줄:
```
- ✅ 디자인 시스템(DESIGN.md)의 색상/폰트는 **A5 최적화 단계**에서 삽입됨
```
→ 변경:
```
- ✅ 디자인 시스템은 Stitch가 D3에서 자체 생성 — A4/A5에서 디자인 토큰 삽입 불필요
```

313번 줄:
```
- ✅ A4는 "무엇을 보여줄지"에 집중, "어떻게 보일지"는 A5+D2에서 처리
```
→ 변경:
```
- ✅ A4는 "무엇을 보여줄지"에 집중, "어떻게 보일지"는 Stitch가 자체 결정 (D3에서 디자인 시스템 자동 생성)
```

- [ ] **Step 2: A5 단일 모드 DESIGN.md 참조 제거**

370-376번 줄 (단일 모드):
```
Skill("enhance-prompt") 호출 1회
→ A4에서 작성한 원시 프롬프트를 전달
→ enhance-prompt 스킬이 .stitch/DESIGN.md 또는 .prism/directions/{direction}/DESIGN.md를 자동 Read
→ 결과를 .prism/directions/default/prompts.md에 저장
```
→ 변경:
```
Skill("enhance-prompt") 호출 1회
→ A4에서 작성한 원시 프롬프트를 전달
→ DESIGN.md 경로 전달 안 함 (디자인 토큰은 Stitch가 D3에서 자체 결정)
→ 결과를 .prism/directions/default/prompts.md에 저장
```

- [ ] **Step 3: A5 멀티 모드 DESIGN.md 경로 명시 제거**

378-384번 줄 (멀티 모드):
```
각 Direction에 대해 Skill("enhance-prompt") 호출:
→ 원시 프롬프트 + Direction Context 블록 삽입
→ DESIGN.md 경로를 명시: ".prism/directions/{direction-name}/DESIGN.md"
→ 결과를 .prism/directions/{direction-name}/prompts.md에 저장
```
→ 변경:
```
각 Direction에 대해 Skill("enhance-prompt") 호출:
→ 원시 프롬프트 + Direction Context 블록 삽입
→ DESIGN.md 경로 전달 안 함 (디자인 토큰은 Stitch가 D3에서 자체 결정)
→ 결과를 .prism/directions/{direction-name}/prompts.md에 저장
```

- [ ] **Step 4: Commit**

```bash
git add skills/prism/references/workflows.md
git commit -m "refactor: remove DESIGN.md references from A4/A5 sections"
```

---

### Task 4: workflows.md — 이중 루프 + Direction Routing 업데이트

**Files:**
- Modify: `skills/prism/references/workflows.md:474-630` (Direction Routing, 이중 루프)

- [ ] **Step 1: Direction Routing 섹션 업데이트**

474-478번 줄:
```
1. `.prism/directions/` 에서 현재 Direction 디렉토리 확인
2. 해당 Direction의 `prompts.md`에서 Feature 프롬프트 로드
3. 해당 Direction의 `DESIGN.md`를 `./DESIGN.md`로 복원 (첫 Feature 이후)
```
→ 변경:
```
1. `.prism/directions/` 에서 현재 Direction 디렉토리 확인
2. 해당 Direction의 `prompts.md`에서 Feature 프롬프트 로드
3. 해당 Direction의 `design-identity.md` 존재 시 Read → 앵커 문구 준비
```

- [ ] **Step 2: 이중 루프 다이어그램 업데이트**

616-630번 줄 이중 루프 블록:
```
Feature 1:
  Direction A → D2(design-md) → DESIGN.md 보존 → D3(새 프로젝트) → D4-D6 → VERIFIED
  Direction B → D2(design-md) → DESIGN.md 보존 → D3(새 프로젝트) → D4-D6 → VERIFIED
  Direction C → D2(design-md) → DESIGN.md 보존 → D3(새 프로젝트) → D4-D6 → VERIFIED
Feature 2:
  Direction A → DESIGN.md 복원 → D3(새 프로젝트, 같은 DESIGN.md) → D4-D6 → VERIFIED
  Direction B → DESIGN.md 복원 → D3(새 프로젝트, 같은 DESIGN.md) → D4-D6 → VERIFIED
  Direction C → DESIGN.md 복원 → D3(새 프로젝트, 같은 DESIGN.md) → D4-D6 → VERIFIED
```
→ 변경:
```
Feature 1:
  Direction A → D3(첫 화면 → 이름 추출 → identity 저장 → 나머지) → D4-D6 → VERIFIED
  Direction B → D3(첫 화면 → 이름 추출 → identity 저장 → 나머지) → D4-D6 → VERIFIED
  Direction C → D3(첫 화면 → 이름 추출 → identity 저장 → 나머지) → D4-D6 → VERIFIED
Feature 2:
  Direction A → identity Read → D3(앵커 포함 프롬프트로 전체 생성) → D4-D6 → VERIFIED
  Direction B → identity Read → D3(앵커 포함 프롬프트로 전체 생성) → D4-D6 → VERIFIED
  Direction C → identity Read → D3(앵커 포함 프롬프트로 전체 생성) → D4-D6 → VERIFIED
```

630번 줄:
```
**핵심:** 같은 Direction 내의 모든 Feature 프로젝트는 동일한 DESIGN.md를 공유한다.
```
→ 변경:
```
**핵심:** 같은 Direction 내의 모든 Feature 프로젝트는 동일한 디자인 시스템 이름 앵커를 공유한다.
```

- [ ] **Step 3: Commit**

```bash
git add skills/prism/references/workflows.md
git commit -m "refactor: update dual-loop and Direction Routing for name anchor"
```

---

### Task 5: SKILL.md — D2 관련 설명 제거 + design-identity.md 추가

**Files:**
- Modify: `skills/prism/SKILL.md`

- [ ] **Step 1: Prerequisites에서 design-md 스킬 설치 제거**

29번 줄 제거:
```bash
npx skills add google-labs-code/stitch-skills --skill design-md --global 2>/dev/null
```

21-33번 줄의 "공식 스킬 3개" → "공식 스킬 2개"로 변경.

- [ ] **Step 2: Design Pipeline 섹션에서 D2 관련 설명 업데이트**

84-89번 줄:
```
Phase D1-D6을 따른다:
- D2: Skill("design-md") → 디자인 시스템 생성
- D3: Skill("stitch-design") → 디자인 생성
- D4: prism이 직접 검증 (읽기 전용 MCP)
- D5: Skill("stitch-design") → 수정
```
→ 변경:
```
Phase D1-D6을 따른다 (D2는 제거됨):
- D3: Design Identity 판단 + Skill("stitch-design") → 디자인 생성
- D4: prism이 직접 검증 (읽기 전용 MCP)
- D5: Skill("stitch-design") → 수정
```

- [ ] **Step 3: DESIGN.md 스와핑 섹션 제거**

151-157번 줄 "## DESIGN.md 스와핑" 전체 섹션 삭제. 아래로 교체:

```markdown
## Design Identity

디자인 시스템 이름은 D3에서 Stitch가 자동 생성한다.

**판단 기준:** `.prism/directions/{direction}/design-identity.md` 존재 여부
- **미존재**: D3 첫 화면 생성 후 `get_project` → `designTheme`에서 이름 + 메타데이터 추출 → 저장
- **존재**: Read → 앵커 문구를 프롬프트에 삽입

이름 추출 소스:
1. `outputComponents` 텍스트: `'{NAME}' design system/aesthetic/palette` 패턴
2. `designTheme.designMd` 첫 `#` 헤딩 파싱 (더 안정적)
```

- [ ] **Step 4: 파일 구조 섹션 업데이트**

134-147번 줄의 파일 구조에서 `DESIGN.md` → `design-identity.md`로 변경, `./DESIGN.md` 제거:

```markdown
## 파일 구조 — .prism/directions/

단일/멀티 모두 동일한 `directions/` 구조:

```
.prism/
  analysis.md                    ← 공통 (A1-A4)
  directions/
    default/                     ← --directions 1
      prompts.md, design-identity.md, project-ids.md
    {direction-name}/            ← --directions N
      prompts.md, design-identity.md, project-ids.md
```

Direction 정리: `rm -rf .prism/directions/{name}/`
```

- [ ] **Step 5: Commit**

```bash
git add skills/prism/SKILL.md
git commit -m "refactor: remove D2, add Design Identity to SKILL.md"
```

---

### Task 6: commands/prism.md — D2 스킬 호출 설명 업데이트

**Files:**
- Modify: `commands/prism.md`

- [ ] **Step 1: `/prism design` 실행 절차에서 D2 참조 제거**

71-76번 줄:
```
4. **Phase D1-D6 실행**:
   - D1: analysis.md 로드 (prism)
   - D2: Skill("design-md") → 디자인 시스템
   - D3: Skill("stitch-design") → 디자인 생성
   - D4-D6: 검증 루프 (Stop hook 자동)
```
→ 변경:
```
4. **Phase D1-D6 실행** (D2는 제거됨):
   - D1: analysis.md 로드 (prism)
   - D3: Design Identity 판단 + Skill("stitch-design") → 디자인 생성
   - D4-D6: 검증 루프 (Stop hook 자동)
```

- [ ] **Step 2: 멀티 모드 설명 업데이트**

77번 줄:
```
> **멀티 모드 (`--directions N`)**: A4.5에서 N개 Direction 생성 후 각 Direction별 D1-D6 순차 실행. workflows.md 참조.
```
이 줄은 그대로 유지 (D1-D6 번호 유지이므로).

- [ ] **Step 3: Commit**

```bash
git add commands/prism.md
git commit -m "refactor: update /prism design command for D2 removal"
```

---

### Task 7: design-verify-stop.sh — Direction 전환 메시지 업데이트

**Files:**
- Modify: `hooks/scripts/design-verify-stop.sh:86-91`

- [ ] **Step 1: 89번 줄 Direction 전환 reason 업데이트**

현재 89번 줄 (heredoc 내부):
```
"reason": "Direction '${CURRENT_DIRECTION}' 검증 완료! (${NEXT_DIR_INDEX}/${TOTAL_DIRECTIONS})\\n\\n다음 Direction: '${NEXT_DIRECTION}'\\n\\n1. Read .claude/prism-design-pipeline.local.md → 현재 direction 확인\\n2. .prism/directions/${NEXT_DIRECTION}/DESIGN.md → ./DESIGN.md 복원 또는 D2 실행\\n3. .prism/directions/${NEXT_DIRECTION}/prompts.md 로드\\n4. Skill(stitch-design)으로 디자인 생성\\n5. 생성 완료 후 phase를 verify로 변경\\n\\nreferences/workflows.md Design Pipeline 절차를 따르세요."
```

→ 변경:
```
"reason": "Direction '${CURRENT_DIRECTION}' 검증 완료! (${NEXT_DIR_INDEX}/${TOTAL_DIRECTIONS})\\n\\n다음 Direction: '${NEXT_DIRECTION}'\\n\\n1. Read .claude/prism-design-pipeline.local.md → 현재 direction 확인\\n2. .prism/directions/${NEXT_DIRECTION}/design-identity.md 존재 시 Read → 앵커 준비\\n3. .prism/directions/${NEXT_DIRECTION}/prompts.md 로드\\n4. Skill(stitch-design)으로 디자인 생성 (앵커 포함)\\n5. 생성 완료 후 phase를 verify로 변경\\n\\nreferences/workflows.md Design Pipeline 절차를 따르세요."
```

- [ ] **Step 2: Commit**

```bash
git add hooks/scripts/design-verify-stop.sh
git commit -m "fix: update stop hook Direction transition for Design Identity"
```

---

### Task 8: 최종 검증 + 버전 업데이트

**Files:**
- Modify: `.claude-plugin/plugin.json`

- [ ] **Step 1: 전체 DESIGN.md 참조 잔여 확인**

```
Grep: "DESIGN\.md" in skills/prism/
Grep: "design-md" in skills/prism/
Grep: "DESIGN\.md" in commands/
Grep: "DESIGN\.md" in hooks/
```

D2 관련 참조가 남아있으면 제거.

- [ ] **Step 2: design-identity.md 참조 일관성 확인**

```
Grep: "design-identity" in skills/prism/
Grep: "design-identity" in hooks/
```

모든 참조가 올바른 경로 `.prism/directions/{direction}/design-identity.md`를 가리키는지 확인.

- [ ] **Step 3: 버전 업데이트**

`.claude-plugin/plugin.json`의 `version`을 `"2.10.0"`으로 변경 (D2 제거는 기능적 변경이므로 minor bump).

- [ ] **Step 4: 스펙 상태 업데이트**

`docs/superpowers/specs/2026-03-22-d2-removal-design.md`의 `Status: Review` → `Status: Implemented`로 변경.

- [ ] **Step 5: Commit**

```bash
git add .claude-plugin/plugin.json docs/superpowers/specs/2026-03-22-d2-removal-design.md
git commit -m "feat: v2.10.0 — D2 제거, Design System Name Anchor 도입"
```
